import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // GLB/GLTF files are served statically from /public/models/
  // No extra webpack config needed for that pattern.
}

export default nextConfig
